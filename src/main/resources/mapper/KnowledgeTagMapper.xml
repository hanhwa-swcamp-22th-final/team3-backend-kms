<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ohgiraffers.team3backendkms.kms.query.mapper.KnowledgeTagMapper">

    <!-- 전체 태그 목록 조회 -->
    <select id="findAllTags"
            resultType="com.ohgiraffers.team3backendkms.kms.query.dto.KnowledgeTagReadDto">
        SELECT
            tag_id   AS tagId,
            tag_name AS tagName
        FROM knowledge_tag
        ORDER BY tag_name
    </select>

    <select id="findTagsByArticleId"
            parameterType="long"
            resultType="com.ohgiraffers.team3backendkms.kms.query.dto.KnowledgeTagReadDto">
        SELECT
            kt.tag_id   AS tagId,
            kt.tag_name AS tagName
        FROM knowledge_article_tag kat
        INNER JOIN knowledge_tag kt ON kat.tag_id = kt.tag_id
        WHERE kat.article_id = #{articleId}
        ORDER BY kt.tag_name
    </select>

</mapper>
